{
  "name": "react-vite-plugin",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "lerna run --stream --scope ui --scope core dev"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "lerna": "^8.1.8"
  },
  "dependencies": {
    "@mastergo/plugin-typings": "^2.5.0"
  }
}
