{
  "name": "core",
  "private": false,
  "version": "0.0.0",
  "scripts": {
    "dev": "tsc && vite build -w",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "common": "workspace:*",
    "@mastergo/plugin-typings": "^2.5.0"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.8.0"
  }
}
